# TODO

추가로 구현해야 할 기능 모음. 각 항목별 별도 일정 산정 필요.

## 1. 대시보드 — 내역 다운로드

- [ ] 대시보드의 KPI/충전 이력/매출 데이터를 **엑셀, PDF**로 다운로드하는 버튼 추가
- [ ] 다운로드 시 기간 선택(이번 달/전월/사용자 지정)
- [ ] 권한별로 노출 데이터 다름 (매출은 `view:revenue`)

**위치**: 대시보드 페이지 상단 우측 액션 영역
**이유**: 보고서 작성/감사 자료 제출 시 매번 운영팀에 자료 요청 → 셀프 다운로드로 해소

---

## 2. 회원 생성 — 엑셀 템플릿 다운로드 및 업로드

- [ ] 신규 계정 등록 페이지에 **엑셀 템플릿 다운로드** 버튼
  - 컬럼: 이름, 이메일, 전화, 사업자번호, 역할, 충전소 매핑 등
- [ ] 엑셀 **업로드 → 검증 → 일괄 등록** 흐름
  - 검증 단계에서 중복/형식 오류 사전 확인
  - 발송 채널(메일/문자/카카오톡) 일괄 선택
- [ ] 발급 결과(성공/실패/임시 비번) 결과 시트 다운로드

**위치**: 신규 계정 등록 페이지
**이유**: 17,000명 일괄 발급 시 한 명씩 입력 불가능. 운영자가 엑셀로 관리하던 명단을 그대로 사용

---

## 3. 메일/문자 발송 — 수신자 직접 지정 시 자동완성

- [ ] "직접 지정" 모드에서 입력창 아래에 **사용자 검색 결과 드롭다운** 표시
  - 입력값으로 이름/이메일/사업자번호 검색
  - 클릭 시 수신자 목록에 추가
- [ ] 추가된 수신자는 칩(chip) 형태로 표시, x 버튼으로 제거
- [ ] UUID 직접 입력도 그대로 지원 (운영자 편의)

**위치**: `/customer/messaging` 직접 지정 입력창
**이유**: 현재 UUID를 콤마/공백으로 구분해 직접 입력해야 함 → 오타/잘못된 ID 발송 사고 위험

---

## 별도 트랙 (보고 NextSteps 참조)

- 계약 정보 화면 표시 여부 결정 (Salesforce 연동 후)
- 인앱 알림 센터
- AI 챗봇, AI 리포트
